import math
class Solution:
    def gcd(self, n, arr):
        # code here 
        i=2
        if n==1:
            return arr[0]
        else:
            a=math.gcd(arr[0],arr[1])
            while(i<n):
                a=math.gcd(a,arr[i])
                i+=1
            return a
